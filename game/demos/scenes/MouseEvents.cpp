#include "MouseEvents.h"

namespace Wheel
{
    namespace Game
    {
        void MouseEvents::Initialize(Core::Scene* scene)
        {
        }

        void MouseEvents::GetBaseMaterial(uint32_t baseMaterial)
        {
            m_BaseMaterial = baseMaterial;
        }
    }
}